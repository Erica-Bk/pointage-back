package com.example.pointage.repository;

import com.example.pointage.model.Suplementaire;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SuplementaireRepository extends JpaRepository<Suplementaire, Long> {
}